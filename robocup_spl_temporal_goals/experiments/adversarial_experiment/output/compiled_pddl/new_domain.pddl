(define (domain robocupdeterministic)
    (:requirements :conditional-effects :derived-predicates :negative-preconditions :non-deterministic :strips :typing)
    (:types location movable)
    (:predicates (Oballdribbledto_leftflank) (balldribbledto) (ballkickedto) (ballkickedto_goaltarget) (goalscored) (isat ?mov ?loc) (isball ?mov) (isrobot ?mov) (movedto) (precedes) (val_Oballdribbledto_leftflank) (val_Yballkickedto_goaltarget) (val_balldribbledto_leftflank) (val_ballkickedto_goaltarget) (val_isat_ball_goaltarget) (val_isat_ball_goaltarget-and-Yballkickedto_goaltarget-and-Oballdribbledto_leftflank))
    (:derived (val_Oballdribbledto_leftflank) (or (val_balldribbledto_leftflank) (Oballdribbledto_leftflank)))
     (:derived (val_Yballkickedto_goaltarget) (ballkickedto_goaltarget))
     (:derived (val_balldribbledto_leftflank) (balldribbledto leftflank))
     (:derived (val_ballkickedto_goaltarget) (ballkickedto goaltarget))
     (:derived (val_isat_ball_goaltarget) (isat ball goaltarget))
     (:derived (val_isat_ball_goaltarget-and-Yballkickedto_goaltarget-and-Oballdribbledto_leftflank) (and (val_isat_ball_goaltarget) (val_Yballkickedto_goaltarget) (val_Oballdribbledto_leftflank)))
    (:action carryball
        :parameters (?rob ?b ?from ?to)
        :precondition (and (isball ?b) (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (isat ?b ?from) (not (isat ?b ?to)) (or (precedes ?from ?to) (initialposition ?from)))
        :effect (and (and (not (isat ?rob ?from)) (isat ?rob ?to) (not (isat ?b ?from)) (isat ?b ?to) (balldribbledto ?to)) (when (val_Oballdribbledto_leftflank) (Oballdribbledto_leftflank)) (when (val_ballkickedto_goaltarget) (ballkickedto_goaltarget)))
    )
     (:action kickball
        :parameters (?rob ?b ?from ?to)
        :precondition (and (isball ?b) (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (isat ?b ?from) (not (isat ?b ?to)) (or (precedes ?from ?to) (initialposition ?from)))
        :effect (and (and (isat ?rob ?from) (not (isat ?b ?from)) (isat ?b ?to) (ballkickedto ?to)) (when (val_Oballdribbledto_leftflank) (Oballdribbledto_leftflank)) (when (val_ballkickedto_goaltarget) (ballkickedto_goaltarget)))
    )
     (:action moverobot
        :parameters (?rob ?from ?to)
        :precondition (and (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)))
        :effect (and (and (not (isat ?rob ?from)) (isat ?rob ?to) (movedto ?to)) (when (val_Oballdribbledto_leftflank) (Oballdribbledto_leftflank)) (when (val_ballkickedto_goaltarget) (ballkickedto_goaltarget)))
    )
)