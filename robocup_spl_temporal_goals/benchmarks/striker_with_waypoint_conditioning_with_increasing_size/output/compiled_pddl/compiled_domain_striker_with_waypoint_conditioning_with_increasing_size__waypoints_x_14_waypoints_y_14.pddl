(define (domain robocupdeterministic)
    (:requirements :conditional-effects :derived-predicates :negative-preconditions :strips :typing)
    (:types location movable)
    (:predicates (Oisat_robot_wpc7r7) (adjacent ?loc1 ?loc2) (isat ?mov ?loc) (isball ?mov) (isrobot ?mov) (val_Oisat_robot_wpc7r7) (val_isat_ball_wpc13r7) (val_isat_ball_wpc13r7-and-Oisat_robot_wpc7r7) (val_isat_robot_wpc7r7))
    (:derived (val_Oisat_robot_wpc7r7) (or (val_isat_robot_wpc7r7) (Oisat_robot_wpc7r7)))
     (:derived (val_isat_ball_wpc13r7) (isat ball wpc13r7))
     (:derived (val_isat_ball_wpc13r7-and-Oisat_robot_wpc7r7) (and (val_isat_ball_wpc13r7) (val_Oisat_robot_wpc7r7)))
     (:derived (val_isat_robot_wpc7r7) (isat robot wpc7r7))
    (:action carryball
        :parameters (?rob ?b ?from ?to)
        :precondition (and (isball ?b) (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (isat ?b ?from) (not (isat ?b ?to)) (or (adjacent ?from ?to) (adjacent ?to ?from)))
        :effect (and (and (not (isat ?rob ?from)) (isat ?rob ?to) (not (isat ?b ?from)) (isat ?b ?to)) (when (val_Oisat_robot_wpc7r7) (Oisat_robot_wpc7r7)))
    )
     (:action kickball
        :parameters (?rob ?b ?from ?to)
        :precondition (and (isball ?b) (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (isat ?b ?from) (not (isat ?b ?to)) (or (adjacent ?from ?to) (adjacent ?to ?from)))
        :effect (and (and (isat ?rob ?from) (not (isat ?b ?from)) (isat ?b ?to)) (when (val_Oisat_robot_wpc7r7) (Oisat_robot_wpc7r7)))
    )
     (:action moverobot
        :parameters (?rob ?from ?to)
        :precondition (and (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (or (adjacent ?from ?to) (adjacent ?to ?from)))
        :effect (and (and (not (isat ?rob ?from)) (isat ?rob ?to)) (when (val_Oisat_robot_wpc7r7) (Oisat_robot_wpc7r7)))
    )
)