(define (domain robocupdeterministic)
    (:requirements :conditional-effects :derived-predicates :negative-preconditions :strips :typing)
    (:types location movable)
    (:predicates (Oisat_robot_wpc3r3) (adjacent ?loc1 ?loc2) (isat ?mov ?loc) (isball ?mov) (isrobot ?mov) (val_Oisat_robot_wpc3r3) (val_isat_ball_wpc5r3) (val_isat_ball_wpc5r3-and-Oisat_robot_wpc3r3) (val_isat_robot_wpc3r3))
    (:derived (val_Oisat_robot_wpc3r3) (or (val_isat_robot_wpc3r3) (Oisat_robot_wpc3r3)))
     (:derived (val_isat_ball_wpc5r3) (isat ball wpc5r3))
     (:derived (val_isat_ball_wpc5r3-and-Oisat_robot_wpc3r3) (and (val_isat_ball_wpc5r3) (val_Oisat_robot_wpc3r3)))
     (:derived (val_isat_robot_wpc3r3) (isat robot wpc3r3))
    (:action carryball
        :parameters (?rob ?b ?from ?to)
        :precondition (and (isball ?b) (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (isat ?b ?from) (not (isat ?b ?to)) (or (adjacent ?from ?to) (adjacent ?to ?from)))
        :effect (and (and (not (isat ?rob ?from)) (isat ?rob ?to) (not (isat ?b ?from)) (isat ?b ?to)) (when (val_Oisat_robot_wpc3r3) (Oisat_robot_wpc3r3)))
    )
     (:action kickball
        :parameters (?rob ?b ?from ?to)
        :precondition (and (isball ?b) (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (isat ?b ?from) (not (isat ?b ?to)) (or (adjacent ?from ?to) (adjacent ?to ?from)))
        :effect (and (and (isat ?rob ?from) (not (isat ?b ?from)) (isat ?b ?to)) (when (val_Oisat_robot_wpc3r3) (Oisat_robot_wpc3r3)))
    )
     (:action moverobot
        :parameters (?rob ?from ?to)
        :precondition (and (isrobot ?rob) (isat ?rob ?from) (not (isat ?rob ?to)) (or (adjacent ?from ?to) (adjacent ?to ?from)))
        :effect (and (and (not (isat ?rob ?from)) (isat ?rob ?to)) (when (val_Oisat_robot_wpc3r3) (Oisat_robot_wpc3r3)))
    )
)