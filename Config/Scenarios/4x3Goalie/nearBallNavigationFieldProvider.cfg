//Parameters loaded by the BallCarrierPFModel provider module
//Implemented by Emanuele Musumeci (musumeci.1653885@studenti.uniroma1.it)

GRAPHICAL_DEBUG = 1; //BOOLEAN 1 or 0
SHOW_TILES = 0; //BOOLEAN 1 or 0
GRAPHICAL_NORM_FACTOR = 3;
GRAPHICAL_POTENTIAL_UPPER_BOUND = 300;
GRAPHICAL_POTENTIAL_LOWER_BOUND = 0;

GRAPHICAL_MAX_MESH_HEIGHT = 510;
GRAPHICAL_MIN_MESH_HEIGHT = 10;

GRAPHICAL_ARROW_LENGTH_AS_NORM = 0; //BOOLEAN 1 or 0
GRAPHICAL_ARROW_LENGTH = 100; //if GRAPHICAL_ARROW_LENGTH_AS_NORM = 0 use a fixed arrow length

ANGLE_BASED_COLOR = true;   //Use potential vector angle to determine arrow color

ATTRACTIVE_FIELD_DELAY = 300; //milliseconds delay between two subsequent updates of the attractive field
REPULSIVE_FIELD_DELAY = 300; //milliseconds delay between two subsequent updates of the attractive field
POTENTIAL_FIELD_DELAY = 300;

FIELD_BORDER_OFFSET = 100; //Offset from field border for any potential field cell

MINIMUM_CELL_SIZE = 100;
MAXIMUM_CELL_SIZE = 150;


USE_DYNAMIC_APPROACH_POINT = false; // Use the ball carrier dynamic approach point as an attractor, otherwise use the static one
//ORA
//PF parameters
RO = 800;   //Radius at which attractive field LINEAR to QUADRATIC (when the field source nearer than r0 to the attractor a conic or linear attraction is received, if further, a quadratic attraction is received)
Kap = 0.1;  //Conic (linear) attractive field strength multiplier
Kbp = 100;  //Quadratic attractive field strength multiplier
TEAMMATE_CO = 500; //NOT USED

Kr = 120;   //Quadratic repulsive field strength multiplier
GAMMA = 2.3;  //Repulsive field exponential intensity (REPULSIVE FIELD INTENSITY)
ETA = 750; //Radius at which repulsive field goes from QUADRATIC to LINEAR (similar but complimentary to R0 radius)
ETA_BALL = 400; //Repulsive field QUADRATIC to LINEAR radius (similar but complimentary to R0 radius) FOR THE BALL ONLY
POW = 1000; //Repulsive field multiplicative intensity factor (NOT IMPORTANT)

Kr_ball = 120;   //Quadratic repulsive field strength multiplier
GAMMA_ball = 2.3;  //Repulsive field exponential intensity (REPULSIVE FIELD INTENSITY)
ETA_ball = 750; //Radius at which repulsive field goes from QUADRATIC to LINEAR (similar but complimentary to R0 radius)
POW_ball = 1000; //Repulsive field multiplicative intensity factor (NOT IMPORTANT)