normalWalkSpeed = 0.7;
slowerWalkSpeed = 0.6;
walkToBallWalkSpeed = 1.0;

initialWaitTime = 50;
realignmentBallNotSeenTimeout = 3500;
ballNotSeenTimeout = 1500;
realignmentTimeout = 1500;
FIRST_STEP_ALIGNMENT_TIMEOUT = 1500;

pathChangeTimeoutInsideCenterCircle = 6000;
pathChangeTimeoutOutsideCenterCircle = 3000;


ballPositionChangedThreshold = 800;


approachXRange = {
    min = 100; 
    max = 600;
};

approachYRange = {
    min = -100; 
    max = 100;
};

smallBallAlignmentRange = {
    min = -8;
    max = 8;
};


FIRST_STEP_REALIGN_RANGE = {
    min = -350;
    max = 350;
};


maxKickWaitTime = 5000;

nearbyObstacleRadius = 1500;

goToCenterWhenSearchingForBall = true;

safetyObstacleRadius = 1200;             //To disable arms behind back, put this to zero
dangerObstacleRadius = 600;              //At and below this radius, the robot will walk at slowerWalkSpeed, between this and safetyObstacleRadius, the speed will be proportional to the distance, above safetyObstacleRadius, it will be normalWalkSpeed

robotArmsRange = 300;

DEBUG_MODE = false;

LOOK_LEFT_AND_RIGHT_WHILE_ALIGNING = false;
LOOK_LEFT_AND_RIGHT_WHILE_WALKING_TO_BALL = true;
LOOK_LEFT_AND_RIGHT_WHILE_KICKING = false;
MAX_LOOKING_ANGLE = 45;

LEFT_AND_RIGHT_LOOKING_SPEED = 80;

START_LOOKING_LEFT_AND_RIGHT_AT_X_COORDINATE = 800;

WALK_ON_BALL = true;
USE_IN_WALK_KICKS_FOR_DISTANCE_MORE_THAN = 9000;

USE_FOOT_OFFSET = true;
BALL_KICK_OFFSET_Y = 70;

MAX_OBSTACLE_LOCALIZATION_DRIFT = 500; //also 500

SHOW_CONDITIONS_DEBUG = false   ;

putArmsOnBack = true;
