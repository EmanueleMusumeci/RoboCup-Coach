time_to_stand_before_kick = 700;
time_to_stand_after_kick = 500;
BALL_KICK_OFFSET_X = 230;
BALL_KICK_OFFSET_Y = 50;
approachXRange = { 
    min = 100;    //Used to align to the static approach point
    max = 700;
};

approachYRange = {
    min = -100;
    max = 100;
};

ballThresholdPre = 200.0;
ballThresholdPost = 200.0;


initialWaitTime = 50;  // TODO maybe make a cfg, but wait if we have any common params with the others

smallApproachXRange = {
    min = 210; 
    max = 240;
}; //Used to align very precisely just before kicking

smallApproachYRange = {
    min = -100; 
    max = -40;
};

smallBallAlignmentRange = {
    min = -10;
    max = 10;
};
smallerBallAlignmentRange = {
    min = -5;
    max = 5;
};
maxKickWaitTime = 3000;
minKickWaitTime = 10;

INITIAL_PASS_TARGET_X = 1500;
INITIAL_PASS_TARGET_Y = -1500;

OPPONENT_TOO_NEAR_FOR_KICKOFF_THRESHOLD = 300;

ENABLE_ADVANCED_KICKOFF = true;